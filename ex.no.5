#include <stdio.h>
#include <unistd.h>

int main() {
    int i;

    for(i=0;i<3;i++) {
        if(fork()==0) {
            printf("Child PID=%d PPID=%d\n", getpid(), getppid());
            return 0;
        }
    }

    sleep(2);
    return 0;
}
