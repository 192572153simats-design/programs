#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main() {
    int source, dest, n;
    char buffer[100];

    source = open("source.txt", O_RDONLY);

    if (source == -1) {
        printf("Source file not found.\n");
        return 1;
    }
